from src.ex1 import log_value

// Perform and log output of simple arithmetic operations
func simple_math() {
    // adding 13 +  14
    tempvar x1 = 13 + 14;
    log_value(x1);

    // multiplying 3 * 6
    tempvar x2 = 3 *6;
    log_value(x2);


    // dividing 6 by 2
    tempvar x3 = 6 / 2;
    log_value(x3);

    // dividing 70 by 2
    tempvar x4 = 70 / 2;
    log_value(x4);

    // dividing 7 by 2
    tempvar x5 = 7 / 2;
    log_value(x5);

    return ();
}